generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_PRISMA_URL")
  directUrl = env("POSTGRES_URL_NON_POOLING")
}

model Participant {
  id        Int      @id @default(autoincrement())
  name      String
  surname   String
  dni       String
  email     String
  phone     String
  ticket    String
  branch    String
  createdAt DateTime @default(now())

  @@unique([ticket, branch])
}
